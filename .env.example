# Local dev secrets — copy to .env and fill in. NEVER commit .env.
# In production these arrive as Cloudflare secrets-store bindings (see wrangler.jsonc).
QDRANT_URL=
QDRANT_KEY=
OPENROUTER_KEY=
SECRET=
GOOGLE_ID=
GOOGLE_SECRET=
